import '../styles/styles.scss';

const Header = () => {
    return (
        <div className="header">
            <h1 className="header__title">Rhythm & Binary</h1>
        </div>
    );
};

export default Header;
